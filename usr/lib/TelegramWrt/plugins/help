#!/bin/sh

echo "🆘 TelegramWrt – Help Menu"
echo ""
echo "Available commands:"
echo ""
echo "🔐 Security & Access Control"
echo "/ssh_status       – Check if SSH is active and which port is used"
echo "/open_ports       – List open ports and active services"
echo "/firewall_rules   – Show current firewall rules"
echo "/block_ip         – Block a specific IP address"
echo "/mac_watch        – Alert if unknown MAC addresses connect"
echo "/vpn_status       – Show VPN connection status"

echo ""
echo "📡 Network & Connectivity"
echo "/devices          – List connected devices via DHCP and Wi-Fi"
echo "/wifi_status      – Show wireless interface status"
echo "/wan_ip           – Display public IP and gateway"
echo "/ping_sweep       - Scan subnet for active IPs (confirmation only)"
echo "/ping_test        – Run a ping test to external host"
echo "/dns_status       – Show current DNS configuration"
echo "/dhcp_leases      – List active DHCP leases"
echo "/tcpdump_dns      - Capture DNS traffic for a short time. Shows client IP, DNS server, and requested domain. Only works if DNS is not encrypted."

echo ""
echo "🛠️ System Diagnostics"
echo "/status           – Show bot status and connection"
echo "/uptime           – Display router uptime"
echo "/cpu_load         – Show CPU load and temperature"
echo "/ram_usage        – Display memory usage"
echo "/disk_space       – Show available disk space"
echo "/log_tail         – Show last system log entries"

echo ""
echo "🔄 Management & Automation"
echo "/reboot           – Reboot the router"
echo "/restart_bot      – Restart the Telegram bot"
echo "/update_bot       – Update bot script from remote source"
echo "/backup_config    – Backup OpenWrt configuration"
echo "/restore_config   – Restore configuration from backup"

echo ""
echo "🎛️ Utilities & Customization"
echo "/start            – Show this command list"
echo "/help             – Extended command descriptions"
echo "/opkg_installed    – 📦 List installed packages (names only)"
echo "/opkg_update      – 📦 Update package repositories"
echo "/opkg_install      – 📥 Install a package from repository by name"
echo "/opkg_remove      – 🗑️ Remove an installed package by name"
